# Only connectors and orchestration are enabled by default in the Helm chart.
# All other components (identity, identityKeycloak, identityPostgresql,
# console, webModeler, webModelerPostgresql, optimize, elasticsearch)
# are already disabled by default.

connectors:
  enabled: false

orchestration:
  enabled: false
