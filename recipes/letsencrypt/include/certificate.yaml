apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: camunda-tls
  namespace: <CAMUNDA_NAMESPACE>
spec:
  secretName: <TLS_SECRET_NAME>
  issuerRef:
    name: letsencrypt
    kind: ClusterIssuer
  dnsNames:
    - <YOUR_HOSTNAME>
